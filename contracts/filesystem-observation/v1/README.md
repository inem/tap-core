# tap.filesystem-observation/v1

A bounded public observation translated from host sensor receipts. `known absent`
is distinct from an inspection failure. `kind` describes only an entry observed
by `lstat`; it does not assert validity, freshness, process ownership, or health.
